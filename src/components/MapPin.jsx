// MapPin — Week 3
// Custom coloured Mapbox marker element.
// Stub — will be built in Week 3.

export default function MapPin({ score }) {
  return <div style={{ width: 12, height: 12, borderRadius: '50%', background: 'var(--accent)' }} />;
}
