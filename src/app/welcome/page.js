import LandingPage from '@/components/LandingPage';

export const metadata = {
  title: 'Venture — Find places tourists never find',
  description: 'AI-powered hidden gems travel planner. Discover the spots locals love that most tourists never find.',
};

export default function WelcomePage() {
  return <LandingPage />;
}
